# 🏛️ 广州市政务服务与政策法规 AI 智能专窗 · 开发与集成技术文档

> **版本**：v3.8.1 (Full Progressive Streaming Edition)  
> **适用对象**：前端开发工程师、系统架构师、测试运维人员、毕业设计/实训评审团队  
> **文档定位**：全方位阐述伴随式政务智能问答插件的前端架构、通信协议、真流式逐字打字机实现、全局配置及常见环境排错排查方案。

---

## 目录索引

- [1. 系统架构与设计哲学](#1-系统架构与设计哲学)
  - [1.1 伴随式轻量挂载架构](#11-伴随式轻量挂载架构)
  - [1.2 Shadow DOM 样式沙箱物理隔离](#12-shadow-dom-样式沙箱物理隔离)
  - [1.3 双智能体 (Dual-Agent) 协同中台拓扑](#13-双智能体-dual-agent-协同中台拓扑)
- [2. 核心功能与关键交互特性](#2-核心功能与关键交互特性)
  - [2.1 首 Token 极速响应与渐进式打字机 (Progressive Streaming)](#21-首-token-极速响应与渐进式打字机-progressive-streaming)
  - [2.2 弱网与中断容灾保全机制 (Fail-Safe Retain)](#22-弱网与中断容灾保全机制-fail-safe-retain)
  - [2.3 规范化错误处理机制](#23-规范化错误处理机制)
  - [2.4 智能政务 Markdown 排版与“一键直达申报”胶囊](#24-智能政务-markdown-排版与一键直达申报胶囊)
  - [2.5 多情形歧义状态机 (Ambiguity Decision Tree)](#25-多情形歧义状态机-ambiguity-decision-tree)
- [3. 云端接口规范与通信协议](#3-云端接口规范与通信协议)
  - [3.1 流式对话接口 (`POST /api/chat/stream`)](#31-流式对话接口-post-apichatstream)
  - [3.2 SSE 事件类型全景对照表](#32-sse-事件类型全景对照表)
  - [3.3 同步问答接口 (`POST /api/chat`)](#33-同步问答接口-post-apichat)
  - [3.4 健康检查接口 (`GET /api/health`)](#34-健康检查接口-get-apihealth)
- [4. 前端对接配置与环境切换](#4-前端对接配置与环境切换)
  - [4.1 全局配置项 (`window.GzGovAiConfig`) 详解](#41-全局配置项-windowgzgovaiconfig-详解)
  - [4.2 场景 A：直连云端微服务中台](#42-场景-a直连云端微服务中台)
  - [4.3 场景 B：本地极速开发与离线答辩调试 (`127.0.0.1:6001`)](#43-场景-b本地极速开发与离线答辩调试-1270016001)
- [5. 跨域安全与环境排错手册 (FAQ)](#5-跨域安全与环境排错手册-faq)
  - [5.1 油猴提示“跨域请求被拦截或网络连接失败”排查](#51-油猴提示跨域请求被拦截或网络连接失败排查)
  - [5.2 浏览器报 Mixed Content（HTTPS 页面阻断 HTTP）问题](#52-浏览器报-mixed-contenthttps-页面阻断-http问题)
  - [5.3 代理软件（Clash / VPN）绕路与延迟高问题](#53-代理软件clash--vpn绕路与延迟高问题)
  - [5.4 云端服务器 500 报错与“断线”排查](#54-云端服务器-500-报错与断线排查)
- [6. 工程代码结构与文件索引](#6-工程代码结构与文件索引)

---

## 1. 系统架构与设计哲学

### 1.1 伴随式轻量挂载架构
本项目摒弃了传统政务问答系统需要自建独立展示门户的厚重做法，采用**伴随式无侵入插件化方案**：
- 以 **Tampermonkey 用户脚本 (`gz_gov_ai_assistant.user.js`)** 或 **嵌入脚本 (`gz_assistant_embed.js`)** 的形式运行；
- 用户在浏览 [广州市人民政府门户网](https://www.gz.gov.cn/) 或 [广东政务服务网](https://www.gdzwfw.gov.cn/) 时，系统常驻于页面右下角，提供“边浏览政策、边办事、边咨询导办”的无缝伴随体验；
- **静默待命设计**：进站默认仅以 official “叻仔”徽标悬浮待命，不遮挡正常网页内容，点击后平滑展开交互窗口。

### 1.2 Shadow DOM 样式沙箱物理隔离
政务门户通常具备庞大、深层且带有大量全局重置的 CSS 样式库（例如直接定义全局 `table`、`td`、`button`、`p` 的外边距和背景）。为保证助手界面在任何政务二级页面均保持 1:1 高保真苹果风毛玻璃（Glassmorphism）与官方规范质感：
- 插件全局挂载在独立的 `ShadowRoot` 节点（`attachShadow({ mode: 'open' })`）；
- 宿主网页所有 CSS 对插件内部 DOM 完全穿透失效，插件自身的样式亦绝不反向污染宿主网站；
- 事件捕获采用封闭式分发，有效防范宿主页面抢占输入框焦点或拦截点击事件。

### 1.3 双智能体 (Dual-Agent) 协同中台拓扑
```text
┌─────────────────────────────────────────────────────────────┐
│                 浏览器宿主环境 (政务门户网)                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 油猴插件 / 嵌入脚本 (Shadow DOM 物理隔离沙箱)            │  │
│  │ • 首 Token 实时打字机渲染器 (Progressive Streaming)     │  │
│  │ • GM_xmlhttpRequest 跨域穿透管道                       │  │
│  └──────────────────────────┬────────────────────────────┘  │
└─────────────────────────────┼───────────────────────────────┘
                              │ HTTP / SSE 流式长连接 (端口: 6001)
┌─────────────────────────────▼───────────────────────────────┐
│        【公网 6001】前台 Spring AI 智能业务大脑             │
│  • 会话管理器 (SessionId 保活与多轮指代推导)                 │
│  • 前置意图编译器 (QueryCompiler 意图分流与辖区识别)         │
│  • Spring AI ChatClient 动态调用与 Prompt 模板装配          │
└─────────────────────────────┬───────────────────────────────┘
                              │ 内部高速 RPC / HTTP (端口: 6002)
┌─────────────────────────────▼───────────────────────────────┐
│        【内网 6002】高确定性政务知识计算与精排中台          │
│  • SQLite FTS5 全文倒排检索引擎 (unicode61 分词，<1ms)       │
│  • BGE-Reranker 本地 CPU 级 ONNX 交叉精排 (~110ms)           │
│  • 4.4GB 官方全量规章库与办事 8 要素数据库                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 核心功能与关键交互特性

### 2.1 首 Token 极速响应与渐进式打字机 (Progressive Streaming)
传统前端 SSE 接入往往将数据缓冲在内存变量中，等待整篇回答全部生成完毕（`onload`）后才一次性全量挂载，造成严重的“首字假死感”。本系统重构升级为**真·渐进式流式渲染**：
- **首 Token 响应（TTFT < 1.0s）**：云端吐出首个数据包时，加载动画立即切换为实时正文气泡，并挂载高亮闪烁打字光标 `▌`；
- **逐字追加**：在接收到每一个 `event: chunk` 时，即时增量送入 Markdown 引擎完成局部排版，同时平滑触底滚动；
- **端到端体感延迟直接归零**，用户能实时看到大模型思考打字全过程。

### 2.2 弱网与中断容灾保全机制 (Fail-Safe Retain)
在跨国或弱网公网环境下，长时间的 TCP 流容易发生中途丢包或被中间网关切断。
- **保护逻辑**：如果连接异常中断时，前端已经接收到有效正文（字符数 > 15）：
- **系统绝不清空已有内容**，而是自动切除打字光标，完整保留已打印的全部段落，并在文末追加弱提示：
  `（网络传输中断，已为您展示已接收内容）`
- 彻底避免了“好不容易接收了 400 字，最后 1 秒断开导致全屏白白报错”的雪崩式故障。

### 2.3 规范化错误处理机制
在连接彻底失败且未收到任何有效 Token 时，自动触发官方标准政务提示，屏蔽所有底层堆栈与调试接口：
```text
市民您好！当前政务智能云端服务暂未连通，请稍后再试
```
同时自动隐藏操作栏中的“复制 / 有疑问”按钮，保持政务严肃感与视觉纯洁度。

### 2.4 智能政务 Markdown 排版与“一键直达申报”胶囊
- **蓝白自适应表格**：遇到收入财产对比表、办理材料表时，自动解析为符合政府规范的高对比度条纹表格；
- **“一键直达申报”动作按钮**：正则自动捕获大模型输出中包含的“进入申办 / 在线办理 / 立即申报”等链接，渲染为抢眼的动作胶囊按钮，点击直接在新标签页以 `noopener,noreferrer` 安全直通官方申报页面；
- **智能排版修剪**：自动清洗 Emoji 表情与卡通图标，确保满足严肃政务规范。

### 2.5 多情形歧义状态机 (Ambiguity Decision Tree)
当市民输入泛化模糊短语（如“公积金贷款”、“办税”、“居住证”）时：
1. 知识中台判定命中多情形业务；
2. 服务端返回 `event: ambiguity`，附带具体情形列表（无需调用耗费 Token 的大语言模型）；
3. 前端界面自动渲染情形选择按钮组（如 `[1] 开具结清证明`、`[2] 偿还还贷本息提取`...）；
4. 市民点击编号或情形后，自动以该会话上下文追加提问，直达具体办理流程。

---

## 3. 云端接口规范与通信协议

### 3.1 流式对话接口 (`POST /api/chat/stream`)
* **协议**：HTTP/1.1 Server-Sent Events (`text/event-stream; charset=utf-8`)
* **鉴权**：`Authorization: Bearer <AUTH_TOKEN>`
* **请求体 (JSON)**：
```json
{
  "query": "非广州户籍办理身份证需要什么材料？",
  "sessionId": "gz-session-1789400000000-abcde"
}
```

### 3.2 SSE 事件类型全景对照表

| 事件名称 (`event`) | 载荷字段 (`data`) 格式示例 | 前端响应与渲染处理 |
| :--- | :--- | :--- |
| `session` | `{"sessionId": "416ef296-6e46-..."}` | 同步会话标识，维持多轮会话上下文 |
| `progress` | `{"stage": "RETRIEVING", "message": "正在检索知识底座..."}` | 更新气泡内小字状态提示，反馈处理进度 |
| `matched_item` | `{"itemId": "114401...", "itemName": "补领居民身份证", "deptName": "广州市公安局", "applyUrl": "http://..."}` | 预备政务事项卡片数据，挂载申报入口 |
| `references` | `{"chunks": [{"title": "条例名", "articleLabel": "第X条", "level": "行政法规"}]}` | 提取法定依据出处，生成条文折叠抽屉注脚 |
| `ambiguity` | `{"isAmbiguous": true, "message": "请选择办理情形", "scenarios": [{"index": 1, "label": "情形A"}]}` | 渲染多情形编号按钮组，等待用户交互分支 |
| `chunk` | `{"content": "根据广州市公积金管理规定..."}` | **核心打字机**：实时追加内容并逐字渲染 |
| `done` | `{"completed": true, "totalTimeMs": 1320}` | 结束打字状态，移除光标，展现操作栏 |
| `error` | `{"message": "服务异常详情"}` | 触发错误降级兜底展示 |

### 3.3 同步问答接口 (`POST /api/chat`)
适合非流式场景、离线脚本或批处理自动化测试。
* **请求**：`POST /api/chat`，请求体同上；
* **返回**：标准 JSON 响应包，包含 `reply`、`sessionId`、`latencyMs` 等字段。

### 3.4 健康检查接口 (`GET /api/health`)
* **请求**：`GET http://<SERVER_HOST>:6001/api/health`
* **正常返回示例**：
```json
{
  "status": "UP",
  "service": "Spring Boot 3 + Spring AI Government RAG Brain",
  "port": 6001,
  "version": "1.0.0",
  "timestamp": 1789396591368
}
```

---

## 4. 前端对接配置与环境切换

### 4.1 全局配置项 (`window.GzGovAiConfig`) 详解
在脚本初始化时，通过合并配置对象实现灵活切换：

```javascript
window.GzGovAiConfig = Object.assign({
  // 核心流式对话接口地址
  apiEndpoint: 'http://149.118.133.163:6001/api/chat/stream',
  // 云端安全鉴权令牌 (Bearer)
  authToken: 'gov-rag-sec-2026-auth-token',
  // 健康状态探针地址
  healthEndpoint: 'http://149.118.133.163:6001/api/health',
  // 是否在离线时启用本地 Mock (生产环境推荐 false)
  mockIfOffline: false,
  // 助手名称与官方落款
  assistantName: '叻仔 · 广州政务智能助手',
  authority: '广州市人民政府门户网站',
  organizer: '广州市政务服务和数据管理局',
  hotline: '12345政务服务便民热线'
}, window.GzGovAiConfig || {});
```

### 4.2 场景 A：直连云端微服务中台
用于线上演示或跨网络访问，保持默认配置即可：
```javascript
apiEndpoint: 'http://149.118.133.163:6001/api/chat/stream',
healthEndpoint: 'http://149.118.133.163:6001/api/health',
```

### 4.3 场景 B：本地极速开发与离线答辩调试 (`127.0.0.1:6001`)
在毕业设计答辩、无公网环境或追求极致 0ms 响应体验时，将后端部署在本地并调整配置：
```javascript
apiEndpoint: 'http://127.0.0.1:6001/api/chat/stream',
healthEndpoint: 'http://127.0.0.1:6001/api/health',
```

---

## 5. 跨域安全与环境排错手册 (FAQ)

### 5.1 油猴提示“跨域请求被拦截或网络连接失败”排查
* **根本原因**：油猴首次向外部 IP 发送请求时会触发安全确认弹窗，若误触了“禁止”，油猴将永久静默拦截。
* **解决步骤**：
  1. 打开油猴扩展管理面板；
  2. 点击本脚本名称进入编辑，切换至 **「设置 (Settings)」** 标签页；
  3. 滚动到 **「XHR 安全 (XHR Security)」** 或 **「用户网域白名单」**；
  4. 检查是否存在该 IP，将其状态切换为 **“允许 (Allow)”** 或直接在白名单中添加 `149.118.133.163`。

### 5.2 浏览器报 Mixed Content（HTTPS 页面阻断 HTTP）问题
* **根本原因**：现代浏览器不允许在 HTTPS 页面（`https://www.gz.gov.cn/`）内直接通过原生 `fetch` 或 `XMLHttpRequest` 调用非加密的 `http://...:6001` 接口。
* **解决途径**：
  - **必须使用油猴脚本运行**：油猴扩展的 `@grant GM_xmlhttpRequest` 拥有特权背景层，原生具备绕过浏览器 Mixed Content 拦截的能力；
  - 若需通过纯网页标签注入运行，后端服务器必须配置 SSL 证书并开启 HTTPS（通过 Nginx 反向代理配置 443 端口与证书）。

### 5.3 代理软件（Clash / VPN）绕路与延迟高问题
* **排查发现**：云端服务器位于海外（马来西亚机房），若本地启用了 Clash/Mihomo 的全局代理或规则模式，请求可能会先发往欧美/中国香港节点再转回东南亚，产生超长跨国二次往返。
* **优化建议**：在代理软件规则列表中添加：
  ```text
  IP-CIDR,149.118.133.163/32,DIRECT
  ```
  强制该服务器走直连链路，延迟可大幅降低至 130ms 物理基线。

### 5.4 云端服务器 500 报错与“断线”排查
* **现象**：询问“公积金贷款”能成功返回选项，但问“身份证”或普通问题时立刻断线报错。
* **原因拆解**：
  - “公积金”走的是知识底座多情形分流，**无需调用大模型**；
  - 普通问题需调用阿里云通义千问（DashScope API），若云端配置的 API Key 额度耗尽或欠费，Spring AI 向上抛出 500 异常并断开连接。
* **解决步骤**：登录阿里云百炼控制台查看 API Key 状态并更新重启云端 Spring Boot 进程。

---

## 6. 工程代码结构与文件索引

```text
├── gz_gov_ai_assistant.user.js   # 【核心】Tampermonkey 油猴用户脚本（支持真流式逐字打字机与 Shadow DOM）
├── gz_assistant_embed.js         # 【核心】网页嵌入集成脚本（供普通网页标签直接引入）
├── backend_architecture_guide.md # 【参考】Spring AI 后端架构设计、算法内核与答辩 PPT 指南
├── DEVELOPMENT.md                # 【本文件】完整开发规范、接口定义与技术排错手册
└── README.md                     # 【说明】项目快速上手与简要简介
```
